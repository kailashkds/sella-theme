export declare enum FilterOptionTypes {
  VALUES = "values",
  VARIANTS = "variants",
  MINIMUM = "minimum",
  RANGE = "range"
}
export declare enum FilterOptionInputType {
  CHECKBOX = "checkbox",
  RADIO = "radio"
}
export interface FilterValue {
  key: string;
  count: string;
  value: string;
  from?: number | "*";
  to?: number | "*";
}
export interface Filter {
  label: string;
  key: "categories" | string;
  inputType: FilterOptionInputType;
  type: FilterOptionTypes;
  min?: number;
  max?: number;
  values: Array<FilterValue>;
}
